kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pihole-pihole-claim
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: pihole-storage
  resources:
    requests:
      storage: 5Gi